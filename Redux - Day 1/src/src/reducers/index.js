import changethenumber from "./Updown";

import {combineReducers} from "redux"

const rootReducer = combineReducers({
    changethenumber
    //we can add more reducers here (if available)
})

export default rootReducer;